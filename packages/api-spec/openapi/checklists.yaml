openapi: 3.1.0
info:
  title: FathersNet Checklists Service Contract
  version: 0.1.0
  description: >-
    Skeleton for the pre-arrival/pre-flight checklist service contract
    (AR-003). Planned resources (implementation-plan/06 Phase B): checklist
    definitions, user checklist progress and completions. Contracts are defined
    here BEFORE implementation; paths land with the corresponding work package.
  contact:
    name: FathersNet Engineering
  license:
    name: MIT
    identifier: MIT

servers:
  - url: http://localhost:3000
    description: Local development gateway (nginx terminates TLS in deployment).

security:
  - bearerAuth: []

paths: {}

components:
  securitySchemes:
    bearerAuth:
      $ref: './common.yaml#/components/securitySchemes/bearerAuth'
