openapi: 3.1.0
info:
  title: FathersNet Users Service Contract
  version: 0.1.0
  description: >-
    Skeleton for the user profile service contract (AR-003). Planned resources
    (implementation-plan/06 Phase B): profile CRUD, family/relationship
    management (FR-… family tree), account lifecycle. Contracts are defined
    here BEFORE implementation; paths land with the corresponding work package.
  contact:
    name: FathersNet Engineering
  license:
    name: MIT
    identifier: MIT

servers:
  - url: http://localhost:3000
    description: Local development gateway (nginx terminates TLS in deployment).

security:
  - bearerAuth: []

paths: {}

components:
  securitySchemes:
    bearerAuth:
      $ref: './common.yaml#/components/securitySchemes/bearerAuth'
