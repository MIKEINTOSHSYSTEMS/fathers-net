# =============================================================================
# FathersNet local development stack — Milestone 1 foundation.
# Reference: implementation-plan/12 §2.5 (pinned images). Docker Compose v2.
#
# Run:  docker compose up -d infra        (dependencies only)
#       docker compose up -d --build      (full stack incl. gateway + nginx)
#       ./scripts/dev.sh                  (deps + turbo dev watch mode)
#
# Secrets are read from .env (gitignored); copy .env.example first.
# n8n is DEVELOPMENT-ONLY and must not be deployed (12 §2.5 / §4.1).
# =============================================================================
name: fathersnet

x-common: &common
  networks:
    - fathersnet
  restart: unless-stopped

services:
  postgres:
    <<: *common
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: fathersnet
      POSTGRES_PASSWORD: ${DB_PASSWORD:-local-dev-only}
      POSTGRES_DB: fathersnet
    ports:
      - '5432:5432'
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U fathersnet -d fathersnet']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    <<: *common
    image: redis:7-alpine
    command: ['redis-server', '--appendonly', 'yes']
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

  qdrant:
    <<: *common
    # Pinned to v1.9.7 — the `v1.9` float tag does not exist on Docker Hub.
    image: qdrant/qdrant:v1.9.7
    ports:
      - '6333:6333'
      - '6334:6334'
    volumes:
      - qdrant_data:/qdrant/storage
    healthcheck:
      test: ['CMD-SHELL', 'bash -c "exec 3<>/dev/tcp/127.0.0.1/6333" || exit 1']
      interval: 10s
      timeout: 5s
      retries: 5

  n8n:
    <<: *common
    image: n8nio/n8n:8.66.0
    profiles: ['dev']
    environment:
      N8N_HOST: localhost
      N8N_PORT: 5678
      N8N_PROTOCOL: http
      N8N_USER_MANAGEMENT_DISABLED: 'false'
      GENERIC_TIMEZONE: Africa/Addis_Ababa
    ports:
      - '5678:5678'
    volumes:
      - n8n_data:/home/node/.n8n

  gateway:
    <<: *common
    build:
      context: .
      dockerfile: services/gateway/Dockerfile
    environment:
      ENV: ${ENV:-dev}
      NODE_ENV: production
      LOG_LEVEL: ${LOG_LEVEL:-info}
      FN_PORT: '3000'
      FN_HOST: 0.0.0.0
      FN_SERVICE_NAME: gateway
      FN_CORS_ORIGINS: ${FN_CORS_ORIGINS:-http://localhost:3000,http://localhost:8080}
      FN_REDIS_URL: redis://redis:6379
      FN_STORE_DRIVER: redis
      FN_RATE_LIMIT_DEFAULT: '120'
      FN_RATE_LIMIT_AI: '30'
      FN_RATE_LIMIT_ADMIN_EXPORT: '10'
      FN_RATE_LIMIT_WINDOW_SECONDS: '60'
      FN_IDEMPOTENCY_TTL_SECONDS: '86400'
    ports:
      - '3000:3000'
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'node -e "fetch(\"http://127.0.0.1:3000/healthz\").then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s

  auth:
    <<: *common
    build:
      context: .
      dockerfile: services/auth/Dockerfile
    environment:
      ENV: ${ENV:-dev}
      NODE_ENV: production
      LOG_LEVEL: ${LOG_LEVEL:-info}
      FN_PORT: '3100'
      FN_HOST: 0.0.0.0
      FN_SERVICE_NAME: auth
      FN_REDIS_URL: redis://redis:6379
      FN_STORE_DRIVER: redis
      FN_AUTH_JWT_SECRET: ${FN_AUTH_JWT_SECRET}
      FN_AUTH_ISSUER: ${FN_AUTH_ISSUER:-fathersnet}
      FN_AUTH_AUDIENCE: ${FN_AUTH_AUDIENCE:-fathersnet-api}
      FN_AUTH_ACCESS_TTL_SECONDS: ${FN_AUTH_ACCESS_TTL_SECONDS:-900}
      FN_AUTH_REFRESH_TTL_SECONDS: ${FN_AUTH_REFRESH_TTL_SECONDS:-2592000}
      FN_AUTH_OTP_TTL_SECONDS: ${FN_AUTH_OTP_TTL_SECONDS:-300}
      FN_AUTH_OTP_LENGTH: ${FN_AUTH_OTP_LENGTH:-6}
      FN_AUTH_OTP_MAX_REQUESTS: ${FN_AUTH_OTP_MAX_REQUESTS:-5}
      FN_AUTH_OTP_REQUEST_WINDOW_SECONDS: ${FN_AUTH_OTP_REQUEST_WINDOW_SECONDS:-900}
      FN_AUTH_OTP_MAX_ATTEMPTS: ${FN_AUTH_OTP_MAX_ATTEMPTS:-5}
      FN_AUTH_OTP_LOCKOUT_SECONDS: ${FN_AUTH_OTP_LOCKOUT_SECONDS:-900}
    ports:
      - '3100:3100'
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'node -e "fetch(\"http://127.0.0.1:3100/healthz\").then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s

  users:
    <<: *common
    build:
      context: .
      dockerfile: services/users/Dockerfile
    environment:
      ENV: ${ENV:-dev}
      NODE_ENV: production
      LOG_LEVEL: ${LOG_LEVEL:-info}
      FN_PORT: '3200'
      FN_HOST: 0.0.0.0
      FN_SERVICE_NAME: users
      FN_STORE_DRIVER: postgres
      FN_DATABASE_URL: postgres://fathersnet:${DB_PASSWORD:-local-dev-only}@postgres:5432/fathersnet
      FN_REDIS_URL: redis://redis:6379
      FN_USERS_JWT_SECRET: ${FN_USERS_JWT_SECRET}
      FN_USERS_ISSUER: ${FN_USERS_ISSUER:-fathersnet}
      FN_USERS_AUDIENCE: ${FN_USERS_AUDIENCE:-fathersnet-api}
      FN_USERS_PHONE_ENC_KEY: ${FN_USERS_PHONE_ENC_KEY}
      FN_USERS_PHONE_DIGEST_KEY: ${FN_USERS_PHONE_DIGEST_KEY}
    ports:
      - '3200:3200'
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'node -e "fetch(\"http://127.0.0.1:3200/healthz\").then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"',
        ]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s

  nginx:
    <<: *common
    image: nginx:1.27-alpine
    ports:
      - '8080:80'
      - '8443:443'
    volumes:
      - ./infra/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./infra/docker/certs:/etc/nginx/certs:ro
    depends_on:
      gateway:
        condition: service_healthy
    healthcheck:
      test: ['CMD-SHELL', 'wget -qO- http://127.0.0.1/healthz || exit 1']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pg_data:
  redis_data:
  qdrant_data:
  n8n_data:

networks:
  fathersnet:
    driver: bridge
